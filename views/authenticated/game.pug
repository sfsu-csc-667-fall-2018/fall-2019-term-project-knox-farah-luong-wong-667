extends gamesLayout

block content
  a(href="/lobby"): button.btn.btn-success(type="button") Back To Lobby
  br

  #Game
    .container
      h1= title
      h1 Welcome To Scrabble!! <b> #{username}</b>
      #playerTurn
      #Game-row.row.justify-content-left.align-items-center
        #Game-column.col-md-6
            #Game-box.col-md-12
              #slider
              #wrapper
              #con
              link(rel='stylesheet' href='http://code.jquery.com/ui/1.9.2/themes/base/jquery-ui.css')

      #Game-row.row.justify-content-left.align-items-center
        #slider
        #wrapper
        #gametray
        
    #playerOneScoreContainer
      h3 <b> #{username}</b>
      hr
      #totalScore.text-weight-bold
      #score.text-weight-bold
      br
      h3 #{opponentUsername}
      hr
      #opponentScore
      br
      button( onclick= "submitTurn('#{}')") Submit
      #valid
      #invalidWord
      #turnSubmitted
      #gameOver
    
    #lobby-box.col-md-12
      #lobby-box-scroll
        div#row.justify-content-end
          for result in gameMessages
            div#row
              if result.User != null
                b.col-md-6 #{result.User.username}
                div.col-md-6
                  p #{result.body}
            
    div
      form(method='POST' action='/api/game/createGameMessage')
        input.w-100(type='text' id='messageBody' placeholder='Send a message' name='messageBody')
        button.w-100.btn.btn-primary(type='submit') Send

