extends layout

block content
  h1= title
  p Welcome to the #{title}

  for result in messages
    div
      b #{result.User.username}
      div
        p #{result.body}
 
  a(href = "/games") Games

